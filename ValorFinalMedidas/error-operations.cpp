#include "stdafx.h"
#include "error-operations.h"

long double inpm_round(long double measurement, unsigned long long decimals) {
	std::cout << measurement << '\n';
	return measurement;
}

int number(int * num) {
	num++;
}